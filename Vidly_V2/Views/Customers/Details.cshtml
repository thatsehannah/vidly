@model Vidly_V2.Models.Customer
@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@Model.Name</h2>
<ul>
    <li>Membership Type: @Model.MembershipType.Name</li>
    @if (Model.Birthday != null)
    {
        <li>@Convert.ToDateTime(Model.Birthday)</li>
    }
</ul>

